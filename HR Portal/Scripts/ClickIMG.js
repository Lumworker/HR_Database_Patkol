$(document).ready(function () {


    $("#Ainputfile0").on('click', function () { $("#inputfile0").click(); });
    $("#inputfile0").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile0 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile1").on('click', function () { $("#inputfile1").click(); });
    $("#inputfile1").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile1 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile2").on('click', function () { $("#inputfile2").click(); });
    $("#inputfile2").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile2 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile3").on('click', function () { $("#inputfile3").click(); });
    $("#inputfile3").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile3 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile4").on('click', function () { $("#inputfile4").click(); });
    $("#inputfile4").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile4 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile5").on('click', function () { $("#inputfile5").click(); });
    $("#inputfile5").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile5 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile6").on('click', function () { $("#inputfile6").click(); });
    $("#inputfile6").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile6 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile7").on('click', function () { $("#inputfile7").click(); });
    $("#inputfile7").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile7 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile8").on('click', function () { $("#inputfile8").click(); });
    $("#inputfile8").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile8 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile9").on('click', function () { $("#inputfile9").click(); });
    $("#inputfile9").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile9 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile10").on('click', function () { $("#inputfile10").click(); });
    $("#inputfile10").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile10 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile11").on('click', function () { $("#inputfile11").click(); });
    $("#inputfile11").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile11 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile12").on('click', function () { $("#inputfile12").click(); });
    $("#inputfile12").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile12 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile13").on('click', function () { $("#inputfile13").click(); });
    $("#inputfile13").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile13 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile14").on('click', function () { $("#inputfile14").click(); });
    $("#inputfile14").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile14 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile15").on('click', function () { $("#inputfile15").click(); });
    $("#inputfile15").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile15 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile16").on('click', function () { $("#inputfile16").click(); });
    $("#inputfile16").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile16 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile17").on('click', function () { $("#inputfile17").click(); });
    $("#inputfile17").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile17 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile18").on('click', function () { $("#inputfile18").click(); });
    $("#inputfile18").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile18 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile19").on('click', function () { $("#inputfile19").click(); });
    $("#inputfile19").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile19 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile20").on('click', function () { $("#inputfile20").click(); });
    $("#inputfile20").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile20 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile21").on('click', function () { $("#inputfile21").click(); });
    $("#inputfile21").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile21 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile22").on('click', function () { $("#inputfile22").click(); });
    $("#inputfile22").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile22 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile23").on('click', function () { $("#inputfile23").click(); });
    $("#inputfile23").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile23 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile24").on('click', function () { $("#inputfile24").click(); });
    $("#inputfile24").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile24 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile25").on('click', function () { $("#inputfile25").click(); });
    $("#inputfile25").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile25 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile26").on('click', function () { $("#inputfile26").click(); });
    $("#inputfile26").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile26 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile27").on('click', function () { $("#inputfile27").click(); });
    $("#inputfile27").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile27 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile28").on('click', function () { $("#inputfile28").click(); });
    $("#inputfile28").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile28 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile29").on('click', function () { $("#inputfile29").click(); });
    $("#inputfile29").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile29 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile30").on('click', function () { $("#inputfile30").click(); });
    $("#inputfile30").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile30 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile31").on('click', function () { $("#inputfile31").click(); });
    $("#inputfile31").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile31 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile32").on('click', function () { $("#inputfile32").click(); });
    $("#inputfile32").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile32 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile33").on('click', function () { $("#inputfile33").click(); });
    $("#inputfile33").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile33 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile34").on('click', function () { $("#inputfile34").click(); });
    $("#inputfile34").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile34 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile35").on('click', function () { $("#inputfile35").click(); });
    $("#inputfile35").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile35 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile36").on('click', function () { $("#inputfile36").click(); });
    $("#inputfile36").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile36 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile37").on('click', function () { $("#inputfile37").click(); });
    $("#inputfile37").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile37 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile38").on('click', function () { $("#inputfile38").click(); });
    $("#inputfile38").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile38 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile39").on('click', function () { $("#inputfile39").click(); });
    $("#inputfile39").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile39 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile40").on('click', function () { $("#inputfile40").click(); });
    $("#inputfile40").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile40 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile41").on('click', function () { $("#inputfile41").click(); });
    $("#inputfile41").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile41 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile42").on('click', function () { $("#inputfile42").click(); });
    $("#inputfile42").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile42 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile43").on('click', function () { $("#inputfile43").click(); });
    $("#inputfile43").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile43 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile44").on('click', function () { $("#inputfile44").click(); });
    $("#inputfile44").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile44 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

    $("#Ainputfile45").on('click', function () { $("#inputfile45").click(); });
    $("#inputfile45").change(function () {
        if (this.files && this.files[0]) {
            var reader = new FileReader();
            reader.onload = function (e) { $('#Ainputfile45 .img-preview').attr('src', e.target.result); };
            reader.readAsDataURL(this.files[0]);
        }
    });

});